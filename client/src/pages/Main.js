import React from 'react';
// import {makeAutoObservable} from "mobx";
import {observer} from "mobx-react-lite";

const Main = observer(() => {
    return (
        <div>
            Main Page
        </div>
    );
})

export default Main;